# Can you change the values inside a list which is contained in set s.


s = {8 , 7 , 12 , "Aditi" , [1,2]}


# no, we cannot change the values inside a list which is contained in set 
# otherwise if the list is independent and was not inside the set then we could have changed the value


